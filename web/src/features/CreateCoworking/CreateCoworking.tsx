import React, { ReactElement } from 'react'
import { Wrapper, style } from './styles'

const CreateCoworking = (): ReactElement => {
    return (
        <Wrapper>
            <div>CreateCoworking</div>
        </Wrapper>
    )
}

export default CreateCoworking
