import React, { ReactElement } from 'react'
import { Wrapper, style } from './styles'
import Card from '../../ui-library/components/Card'

const Login = (): ReactElement => {
    return (
        <Wrapper>
            {/*<div>Login</div>*/}
            <Card>login</Card>
        </Wrapper>
    )
}

export default Login
